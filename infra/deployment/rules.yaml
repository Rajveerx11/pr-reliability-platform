groups:
  - name: pr-reliability-v1
    rules:
      - alert: TelemetryCollectorUnavailable
        expr: up{job="otel-collector"} == 0
        for: 5m
        labels:
          severity: critical
        annotations:
          summary: PR Reliability telemetry collector is unavailable
